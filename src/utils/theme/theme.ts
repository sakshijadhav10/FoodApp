import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#FF5F1F",
      light: "#ffffff",
      // light: "skyblue",
    },
    secondary: {
      main: "#808080",
    },
  },
});
